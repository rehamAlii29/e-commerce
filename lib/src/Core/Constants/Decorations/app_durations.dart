  class AppDurations {
    AppDurations._();
  static const Duration miniButtonToggle = Duration(milliseconds: 50);
  static const Duration buttonToggle = Duration(milliseconds: 200);
  static const Duration mainTransition = Duration(milliseconds: 300);
  static const Duration avgTransition = Duration(milliseconds: 450);
  static const Duration extendedTransition = Duration(milliseconds: 600);
}
